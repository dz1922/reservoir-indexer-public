{
  "name": "@nft-data-hub/adapter",
  "version": "0.0.1",
  "description": "Unified data adapter layer for NFT Data Hub",
  "license": "MIT",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf ./dist tsconfig.tsbuildinfo",
    "compile": "tsc",
    "build": "yarn clean; yarn compile",
    "dev": "tsc --watch",
    "test": "jest --no-watchman"
  },
  "dependencies": {
    "pg-promise": "^10.12.1"
  },
  "devDependencies": {
    "@types/jest": "^29.0.2",
    "jest": "^29.0.3",
    "ts-jest": "^29.0.1",
    "ts-node": "^10.4.0",
    "typescript": "^4.9.5"
  }
}
